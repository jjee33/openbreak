apiVersion: v1
kind: Service
metadata:
  name: openbreak
  namespace: openbreak
  labels:
    app.kubernetes.io/name: openbreak
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openbreak
    app.kubernetes.io/component: web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
